/* Bizzing India — epic episode artwork index. Generated by tools/gen-epic-art.py.
   Keys listed here have a painting at app/art/epic/<epicId>-<n>.jpg (900x600).

   One painting per EPISODE, not per card. The two epics run to 686 cards between them;
   painting each would add roughly 100MB to a 107MB offline-first app, which is a worse
   problem than the cost. Instead the episode's painting is held above the deck and panned
   as the cards turn, so every card carries artwork and the deck reads as one scene being
   walked through — which is how a patachitra scroll is actually read.

   Empty until the generation pass lands; everything that reads it degrades to no image. */
window.IND_EPIC_ART = [];
